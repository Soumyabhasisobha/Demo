name: Hello World
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello Step
        run: "Welcome"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "test"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: "Welcome"
  
