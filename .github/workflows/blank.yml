name: Hello World
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello Step
        run: echo "Welcome"
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "test"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Welcome"
  
